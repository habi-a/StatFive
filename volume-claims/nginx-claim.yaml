apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-claim
  namespace: statfive
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Mi
