apiVersion: v1
kind: Service
metadata:
  labels:
    app: statfive
  name: dashboard
  namespace: statfive
spec:
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
  selector:
    app: statfive
    tier: dashboard
